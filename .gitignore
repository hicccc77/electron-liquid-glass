node_modules/
build/
# Prebuilt binaries: generated by `npm run prebuilds` before npm publish (the
# package.json "files" field puts them in the tarball; git is irrelevant here).
# Binaries stay out of git history.
prebuilds/
*.tgz
.DS_Store
Thumbs.db
*.log
